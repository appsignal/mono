#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH << File.expand_path(File.join(__dir__, "..", "lib"))

require "mono"
require "mono/cli"

command, *options = ARGV
Mono::Cli::Wrapper.new(command, options).execute
